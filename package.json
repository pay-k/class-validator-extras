{
  "name": "@payk/class-validator-extras",
  "version": "1.0.9",
  "description": "Class-Validator extra validation decorators",
  "main": "dist/index.js",
  "repository": {
    "url": "https://github.com/pay-k/class-validator-extras.git",
    "type": "git"
  },
  "scripts": {
    "test": "mocha --opts .mocharc",
    "test-juint": "mocha --opts .mocharc --reporter mocha-junit-reporter",
    "prebuild": "rimraf dist",
    "build": "tsc",
    "publish-package": "npm run-script build && npm publish"
  },
  "keywords": [
    "class-validator",
    "validation",
    "decorators"
  ],
  "author": "Dan Shapir",
  "license": "ISC",
  "dependencies": {
    "typescript": "^3.6.3"
  },
  "devDependencies": {

    "class-validator": "^0.14.0",
    "rimraf": "^3.0.0",
    "source-map-support": "^0.5.13",
    "ts-node": "^8.3.0"
  },
  "peerDependencies": {

    "class-validator": "^0.14.0"
  }
}
